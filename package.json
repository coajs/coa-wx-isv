{
  "name": "coa-wx-isv",
  "version": "0.0.0",
  "description": "wx isv component for coa",
  "keywords": [
    "coajs",
    "coa",
    "wx-isv"
  ],
  "license": "MIT",
  "author": "Aex",
  "homepage": "https://github.com/coajs",
  "repository": {
    "type": "git",
    "url": "https://github.com/coajs/coa-wx-isv.git"
  },
  "scripts": {
    "dev": "tsc -w",
    "build": "rm -rf dist && tsc && cp package.json *.md dist && rm -rf dist/@*",
    "publish-prerelease": "yarn build && yarn version --prerelease && cp package.json *.md dist && cd dist && rm -rf @* && npm publish",
    "test": "node dist/@test"
  },
  "dependencies": {
    "coa-echo": "^1.0.8",
    "coa-error": "^1.1.3",
    "coa-helper": "^1.1.1",
    "coa-xml": "^1.0.2"
  },
  "devDependencies": {
    "@types/node": "^14.14.35",
    "typescript": "^4.2.3"
  }
}
